//my code
function mostVowels (str){
    let max =0
    for (i=0; i<str.length;i++){
        for (let j=0;j)
        count = 0;
        if (str[0]==='a'||str[0]==='e'||str[0]==='i'||str[0]==='o'||str[0]==='u'){
        count+=1;
    }
    }
}
